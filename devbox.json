{
  "packages": [
    "rustup@latest"
  ],
  "shell": {
    "init_hook": [
      "echo 'Welcome to devbox!' > /dev/null"
    ],
    "scripts": {
      "test": [
        "echo \"Error: no test specified\" && exit 1"
      ],
      "start_server": [
        "cd components/server && cargo run -- --help"
      ]
    }
  }
}

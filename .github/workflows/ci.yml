name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]



defaults:
  run:
    shell: bash

permissions:
  contents: read

jobs:
  lint:
    name: Lint
    uses: ./.github/workflows/lint.yml

  rust:
    name: Rust
    uses: ./.github/workflows/rust.yml

  test:
    name: Test
    uses: ./.github/workflows/test.yml

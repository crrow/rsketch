# YTMusic Player Desktop UI Design

## Overview

基于 GPUI 框架的 YouTube Music 桌面客户端 UI 设计。采用 ytmusicapi 风格的非官方 API 获取数据。

## MVP 功能范围

| 功能 | 包含 | 不包含 |
|------|------|--------|
| 播放列表 | 展示、播放、队列管理 | 创建、编辑、删除 |
| 播放器 | 播放/暂停、进度、音量、上下曲 | 循环、随机播放 |
| 主题 | 完全自定义配色 | - |
| 搜索 | - | 跳过（后续迭代） |

## 整体架构

### UI 组件架构

```
yunara-ui/
├── components/
│   ├── layout/           # 布局组件
│   │   ├── app_shell     # 应用主框架（三栏容器）
│   │   ├── sidebar       # 左侧边栏
│   │   ├── content_area  # 中央内容区
│   │   └── queue_panel   # 右侧队列面板（条件显示）
│   │
│   ├── player/           # 播放器组件
│   │   ├── player_bar    # 底部播放控制栏
│   │   ├── progress_slider # 播放进度条（可拖拽）
│   │   ├── volume_control # 音量控制
│   │   └── playback_controls # 播放/暂停/上下曲
│   │
│   ├── media/            # 媒体展示组件
│   │   ├── track_item    # 歌曲列表项
│   │   ├── cover_mosaic  # 专辑封面拼图
│   │   └── playlist_item # 播放列表项
│   │
│   ├── playlist/         # 播放列表组件
│   │   └── playlist_detail # 播放列表详情页
│   │
│   └── theme/            # 主题系统
│       ├── theme_config  # 主题配置结构
│       └── theme_provider # 主题上下文提供者
```

### 数据流

应用状态集中在 `yunara` crate 的 `AppState` 中，UI 组件通过 GPUI 的 `Entity` 系统订阅状态变化并重新渲染。

## 布局结构

### AppShell 主框架

```
┌─────────────────────────────────────────────────────────────────┐
│  Logo    [搜索框]                              [← →] [用户头像] │  <- Header (64px)
├─────────┬───────────────────────────────────────┬───────────────┤
│ Home    │                                       │ UP NEXT      │
│ Explore │                                       │ LYRICS       │
│ Library │      中央内容区                        │ RELATED      │
│─────────│      (路由切换)                        │──────────────│
│ + New   │                                       │ Track 1      │
│─────────│                                       │ Track 2      │
│ Liked   │      - 播放列表详情                    │ Track 3      │
│ List 1  │      - 专辑封面(播放时)                │ ...          │
│ List 2  │                                       │              │
│ ...     │                                       │              │
├─────────┴───────────────────────────────────────┴───────────────┤
│ [◄◄] [▶] [►►]  歌曲名 - 艺术家   ━━━━━●━━━━  2:37/4:01   🔊━━━ │  <- PlayerBar (80px)
└─────────────────────────────────────────────────────────────────┘
```

### 尺寸规格

| 区域 | 宽度 | 说明 |
|------|------|------|
| 左侧边栏 | 240px 固定 | 导航 + 播放列表 |
| 右侧队列 | 320px 固定 | 仅播放时显示 |
| 中央内容 | 弹性填充 | flex: 1 |
| Header | 100% × 64px | 搜索 + 导航 |
| PlayerBar | 100% × 80px | 播放控制 |

### 右侧面板显示逻辑

- 左侧边栏：始终显示
- 右侧队列面板：仅当有正在播放的播放列表时显示

```rust
fn should_show_queue_panel(state: &PlayerState) -> bool {
    state.current_playlist.is_some()
}
```

## 主题系统

### 主题配置结构

```rust
pub struct ThemeConfig {
    // 背景色
    pub background_primary: Rgba,    // 主背景 (如 #121212)
    pub background_secondary: Rgba,  // 侧边栏/面板背景 (如 #1d1d1d)
    pub background_elevated: Rgba,   // 悬浮元素背景 (如 #282828)

    // 文字色
    pub text_primary: Rgba,          // 主要文字 (如 #ffffff)
    pub text_secondary: Rgba,        // 次要文字 (如 #a0a0a0)
    pub text_muted: Rgba,            // 弱化文字 (如 #6a6a6a)

    // 强调色
    pub accent: Rgba,                // 主强调色 (如 #ff0000 YTMusic红)
    pub accent_hover: Rgba,          // 悬停状态

    // 交互色
    pub hover: Rgba,                 // 悬停背景
    pub active: Rgba,                // 激活/选中背景
    pub border: Rgba,                // 边框色

    // 播放器专用
    pub progress_track: Rgba,        // 进度条轨道
    pub progress_fill: Rgba,         // 进度条填充（通常 = accent）
}
```

### 预设主题

| 预设 | 风格 | 主背景 | 强调色 |
|------|------|--------|--------|
| YTMusic Dark | 官方风格 | #121212 | #ff0000 |
| OLED Black | 纯黑省电 | #000000 | #ff0000 |
| Light | 浅色模式 | #ffffff | #ff0000 |

### 主题存储

用户自定义主题保存到 `yunara-store` 数据库，应用启动时加载。通过 `ThemeProvider` 在 GPUI 的 `Global` 中注入，所有组件通过 `cx.global::<ThemeProvider>()` 访问当前主题。

## 组件设计

### PlayerBar 底部控制栏

```
┌────────────────────────────────────────────────────────────────────────┐
│                                                                        │
│  [封面]  歌曲名                    [◄◄] [▶] [►►]           🔊━━━━━━━━  │
│   48px   艺术家名                                          音量滑块    │
│                                                                        │
│  ━━━━━━━━━━━━━━━━━━━━━━━●━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  2:37/4:01  │
│                      进度条（可拖拽）                                   │
└────────────────────────────────────────────────────────────────────────┘
```

```rust
pub struct PlaybackControls {
    is_playing: bool,
    has_previous: bool,
    has_next: bool,
}

pub struct ProgressSlider {
    current_time: Duration,
    total_duration: Duration,
    is_dragging: bool,
    drag_position: Option<f32>,
}

pub struct VolumeControl {
    volume: f32,        // 0.0 - 1.0
    is_muted: bool,
    previous_volume: f32,
}

pub struct NowPlayingInfo {
    track_title: SharedString,
    artist_name: SharedString,
    cover_url: Option<SharedString>,
    cover_image: Option<Arc<RenderImage>>,
}
```

### TrackItem 歌曲列表项

```
┌─────────────────────────────────────────────────────────────────┐
│  [封面]   歌曲标题                                        3:41  │
│   40px    艺术家名 • 专辑名                                 👍   │
└─────────────────────────────────────────────────────────────────┘
```

```rust
pub struct TrackItem {
    pub track: Track,
    pub is_playing: bool,
    pub is_hovered: bool,
    pub show_index: bool,
}

pub struct Track {
    pub id: String,
    pub title: SharedString,
    pub artist: SharedString,
    pub album: Option<SharedString>,
    pub duration: Duration,
    pub cover_url: Option<SharedString>,
}
```

### Sidebar 左侧边栏

```
┌─────────────────────┐
│  🏠 Home            │
│  🔍 Explore         │
│  📚 Library         │
├─────────────────────┤
│  ＋ New playlist    │
├─────────────────────┤
│  ♥ Liked Music      │
│─────────────────────│
│  Japanese           │
│  teng aiden         │
│─────────────────────│
│  BGM                │
│  bigwatermelon      │
│─────────────────────│
│  ...                │
└─────────────────────┘
```

```rust
pub struct Sidebar {
    pub nav_items: Vec<NavItem>,
    pub playlists: Vec<PlaylistSummary>,
    pub selected_playlist_id: Option<String>,
}

pub struct NavItem {
    pub icon: Icon,
    pub label: SharedString,
    pub route: Route,
}

pub struct PlaylistSummary {
    pub id: String,
    pub name: SharedString,
    pub owner: SharedString,
    pub track_count: usize,
}

pub enum Route {
    Home,
    Explore,
    Library,
    Playlist(String),
}
```

### QueuePanel 右侧队列面板

```
┌─────────────────────────┐
│ [UP NEXT] LYRICS RELATED│
├─────────────────────────┤
│  ▶ Track 1 (playing)    │
│    Track 2              │
│    Track 3              │
│    ...                  │
└─────────────────────────┘
```

```rust
pub struct QueuePanel {
    pub active_tab: QueueTab,
    pub queue: Vec<Track>,
    pub current_index: Option<usize>,
}

pub enum QueueTab {
    UpNext,
    Lyrics,
    Related,
}
```

### PlaylistDetail 播放列表详情页

```
┌──────────────────────────────────────────────────────────────────┐
│  [封面拼图]     KPop                              ≡ Sort ▼      │
│   4张专辑       bigwatermelon                                    │
│   封面组合      Playlist • Public • 2025                         │
│                 2 views • 5 tracks • 18 minutes                  │
│                                                                  │
│                 [⬇] [✏] [icons...]  [▶ 播放]  [↗] [⋮]          │
├──────────────────────────────────────────────────────────────────┤
│  [封面] Super Shy                                          2:35  │
│         NewJeans • NewJeans 'Super Shy'                     👍   │
├──────────────────────────────────────────────────────────────────┤
│  ...                                                             │
├──────────────────────────────────────────────────────────────────┤
│  Suggestions                                      [Refresh]      │
│  ...                                                             │
├──────────────────────────────────────────────────────────────────┤
│  Related playlists                                    < >        │
│  [playlist cards...]                                             │
└──────────────────────────────────────────────────────────────────┘
```

```rust
pub struct PlaylistDetail {
    pub playlist: Playlist,
    pub tracks: Vec<Track>,
    pub suggestions: Vec<Track>,
    pub related_playlists: Vec<PlaylistSummary>,
    pub sort_order: SortOrder,
}

pub struct Playlist {
    pub id: String,
    pub name: SharedString,
    pub owner: SharedString,
    pub visibility: Visibility,
    pub year: u16,
    pub view_count: u32,
    pub track_count: usize,
    pub total_duration: Duration,
    pub cover_images: Vec<SharedString>,
}

pub enum SortOrder {
    Default,
    Title,
    Artist,
    DateAdded,
}

pub enum Visibility {
    Public,
    Private,
    Unlisted,
}
```

### CoverMosaic 封面拼图

```rust
pub struct CoverMosaic {
    pub images: Vec<Arc<RenderImage>>,  // 1-4张图
    pub size: f32,
}
// 1张 → 单图
// 2张 → 左右各半
// 3张 → 上1下2
// 4张 → 2x2网格
```

## 状态管理 (yunara crate)

```rust
pub struct PlayerState {
    pub now_playing: Option<NowPlayingInfo>,
    pub playback: PlaybackControls,
    pub progress: ProgressSlider,
    pub volume: VolumeControl,
    pub queue: Vec<Track>,
    pub current_index: Option<usize>,
    pub current_playlist: Option<Playlist>,
}
```

## 文件结构

```
yunara-ui/components/
├── layout/
│   ├── app_shell.rs
│   ├── sidebar.rs
│   ├── content_area.rs
│   └── queue_panel.rs
│
├── player/
│   ├── player_bar.rs
│   ├── progress_slider.rs
│   ├── volume_control.rs
│   └── playback_controls.rs
│
├── media/
│   ├── track_item.rs
│   ├── cover_mosaic.rs
│   └── playlist_item.rs
│
├── playlist/
│   └── playlist_detail.rs
│
└── theme/
    ├── theme_config.rs
    └── theme_provider.rs

yunara/src/state/
├── player_state.rs
├── playlist_state.rs
└── theme_state.rs
```
